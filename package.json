{
  "name": "electron-auto-update-example",
  "version": "1.0.7",
  "description": "",
  "main": "main.js",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/markseng/teste_auto_deploy.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --linux --publish never",
    "deploy": "electron-builder build   --linux --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^5.0.1",
    "electron-builder": "^20.40.2"
  },
  "dependencies": {
    "electron-log": "^1.3.0",
    "electron-updater": "^4.0.6"
  }
}
